[tool.poetry]
name = "repl_python3_snugbot"
version = "0.1.0"
description = ""
authors = ["snugdev <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
twitchio = "^2.0.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
